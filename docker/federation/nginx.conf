events {
    worker_connections 1024;
}

http {
    upstream studycycle-alpha {
        server "studycycle-alpha:8541";
    }
    upstream studycycle-alpha-ui {
        server "studycycle-alpha-ui:1234";
    }
    server {
        listen 8540;
        server_name 127.0.0.1;
        access_log  off;

        # Upload limit for pictshare
        client_max_body_size 50M;

        location ~ ^/(api|pictrs|feeds|nodeinfo|.well-known) {
            proxy_pass http://studycycle-alpha;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
        location / {
            set $proxpass http://studycycle-alpha-ui;
            if ($http_accept = "application/activity+json") {
              set $proxpass http://studycycle-alpha;
            }
            if ($http_accept = "application/ld+json; profile=\"https://www.w3.org/ns/activitystreams\"") {
              set $proxpass http://studycycle-alpha;
            }
            proxy_pass $proxpass;

            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            # Cuts off the trailing slash on URLs to make them valid
            rewrite ^(.+)/+$ $1 permanent;
        }
    }

    upstream studycycle-beta {
        server "studycycle-beta:8551";
    }
    upstream studycycle-beta-ui {
        server "studycycle-beta-ui:1234";
    }
    server {
        listen 8550;
        server_name 127.0.0.1;
        access_log off;

        # Upload limit for pictshare
        client_max_body_size 50M;

        location ~ ^/(api|pictrs|feeds|nodeinfo|.well-known) {
            proxy_pass http://studycycle-beta;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
        location / {
            set $proxpass http://studycycle-beta-ui;
            if ($http_accept = "application/activity+json") {
              set $proxpass http://studycycle-beta;
            }
            if ($http_accept = "application/ld+json; profile=\"https://www.w3.org/ns/activitystreams\"") {
              set $proxpass http://studycycle-beta;
            }
            proxy_pass $proxpass;

            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            # Cuts off the trailing slash on URLs to make them valid
            rewrite ^(.+)/+$ $1 permanent;
        }
    }

    upstream studycycle-gamma {
        server "studycycle-gamma:8561";
    }
    upstream studycycle-gamma-ui {
        server "studycycle-gamma-ui:1234";
    }
    server {
        listen 8560;
        server_name 127.0.0.1;
        access_log off;

        # Upload limit for pictshare
        client_max_body_size 50M;

        location ~ ^/(api|pictrs|feeds|nodeinfo|.well-known) {
            proxy_pass http://studycycle-gamma;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
        location / {
            set $proxpass http://studycycle-gamma-ui;
            if ($http_accept = "application/activity+json") {
              set $proxpass http://studycycle-gamma;
            }
            if ($http_accept = "application/ld+json; profile=\"https://www.w3.org/ns/activitystreams\"") {
              set $proxpass http://studycycle-gamma;
            }
            proxy_pass $proxpass;

            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            # Cuts off the trailing slash on URLs to make them valid
            rewrite ^(.+)/+$ $1 permanent;
        }
    }

    upstream studycycle-delta {
        server "studycycle-delta:8571";
    }
    upstream studycycle-delta-ui {
        server "studycycle-delta-ui:1234";
    }
    server {
        listen 8570;
        server_name 127.0.0.1;
        access_log off;

        # Upload limit for pictshare
        client_max_body_size 50M;

        location ~ ^/(api|pictrs|feeds|nodeinfo|.well-known) {
            proxy_pass http://studycycle-delta;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
        location / {
            set $proxpass http://studycycle-delta-ui;
            if ($http_accept = "application/activity+json") {
              set $proxpass http://studycycle-delta;
            }
            if ($http_accept = "application/ld+json; profile=\"https://www.w3.org/ns/activitystreams\"") {
              set $proxpass http://studycycle-delta;
            }
            proxy_pass $proxpass;

            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            # Cuts off the trailing slash on URLs to make them valid
            rewrite ^(.+)/+$ $1 permanent;
        }
    }

    upstream studycycle-epsilon {
        server "studycycle-epsilon:8581";
    }
    upstream studycycle-epsilon-ui {
        server "studycycle-epsilon-ui:1234";
    }
    server {
        listen 8580;
        server_name 127.0.0.1;
        access_log off;

        # Upload limit for pictshare
        client_max_body_size 50M;

        location ~ ^/(api|pictrs|feeds|nodeinfo|.well-known) {
            proxy_pass http://studycycle-epsilon;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
        location / {
            set $proxpass http://studycycle-epsilon-ui;
            if ($http_accept = "application/activity+json") {
              set $proxpass http://studycycle-epsilon;
            }
            if ($http_accept = "application/ld+json; profile=\"https://www.w3.org/ns/activitystreams\"") {
              set $proxpass http://studycycle-epsilon;
            }
            proxy_pass $proxpass;

            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            # Cuts off the trailing slash on URLs to make them valid
            rewrite ^(.+)/+$ $1 permanent;
        }
    }
}
